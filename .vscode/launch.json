{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SkillVerse",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "env": {
                "FLASK_ENV": "development"
            }
        }
    ]
}